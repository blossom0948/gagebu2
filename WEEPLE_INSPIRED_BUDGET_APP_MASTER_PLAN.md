# Weeple 레퍼런스 기반 AI 가계부 앱 마스터 설계서

> 이 문서는 Codex가 Android Studio 프로젝트를 설계·구현할 때 사용하는 기준 문서다.  
> 목표는 Weeple의 정보 구조와 사용 흐름을 참고하되, 로고·이름·문구·그래픽 자산을 복제하지 않고 독자적인 브랜드와 더 부드러운 사용자 경험을 갖춘 AI 가계부를 만드는 것이다.

## 최우선 조건: 첫 결과물은 실제 실행 가능한 앱

이 문서를 Codex에 처음 전달했을 때 첫 결과물은 화면 미리보기나 디자인 시안이 아니라 **빌드·설치·실행이 가능한 Android 앱**이어야 한다.

- Compose `@Preview`, 스크린샷, Figma, HTML 프로토타입, 정적인 목업만 만든 뒤 완료라고 하지 않는다. 미리보기는 개발 중 보조 수단으로만 허용한다.
- 저장소를 확인한 뒤 계획만 보고하고 멈추지 않는다. 기존 기능은 보존하면서 첫 실행 가능한 vertical slice를 실제 코드로 구현하고 Debug APK까지 빌드한다.
- 첫 APK에는 실제 탐색이 되는 화면과 Room 기반 거래 추가·조회가 포함되어야 한다. 거래를 추가하면 앱을 닫았다 열어도 유지되고 홈/내역에 반영되어야 한다.
- 서버 주소, 로그인, Gemini 키가 아직 없어도 앱의 기본 가계부 기능은 실행 가능해야 한다. 구현되지 않은 AI/동기화 기능은 실제 동작하는 것처럼 꾸미지 말고 명확히 비활성 상태와 다음 설정 방법을 안내한다.
- 가능하면 연결된 Android 기기나 에뮬레이터에 설치해 직접 실행한다. 사용할 수 없다면 Debug APK를 반드시 만들고, 설치 방법 및 실제로 수행하지 못한 검증을 구분해서 보고한다.
- 빌드가 실패했는데 성공했다고 보고하거나, APK 대신 미리보기만 내놓지 않는다. 환경 문제로 APK를 만들 수 없다면 오류와 막힌 조건을 구체적으로 남긴다.

첫 결과물 완료 기준:

1. Android Studio에서 열 수 있는 실제 Kotlin/Compose 프로젝트.
2. Gradle Debug 빌드 성공 및 `app-debug.apk` 생성.
3. 홈·내역 등 주요 하단 탭이 실제 화면 이동으로 작동.
4. 거래 입력 → Room 저장 → 앱 재실행 후에도 거래 확인 가능.
5. 변경 파일, 빌드/테스트 결과, APK 위치, 기기 설치 결과 또는 미실행 이유를 한국어로 보고.

## 0. Codex에 바로 전달할 실행 명령

아래 문장을 이 MD 파일과 함께 Codex에 전달한다.

```text
첨부한 WEEPLE_INSPIRED_BUDGET_APP_MASTER_PLAN.md를 프로젝트의 단일 기준 문서로 사용해라.
먼저 현재 저장소와 기존 구현을 검사하고, 이미 동작하는 코드는 보존해라. Android 프로젝트가 없으면 Kotlin + Jetpack Compose 기반 Android Studio 프로젝트를 실제 파일로 생성해라.
분석이나 계획만 답하고 멈추지 말고, 첫 작업에서 앱이 실제로 설치·실행되도록 구현한 뒤 Debug APK를 빌드해라.
첫 APK에는 실제 화면 이동과 Room에 저장되는 거래 추가·조회 기능을 넣어라. 서버나 API 키 없이도 기본 가계부 기능이 작동해야 한다.
Compose @Preview, 스크린샷, HTML/Figma 시안만 제출하거나 하드코딩 목업 화면으로 대체하지 마라. Preview는 실제 앱 구현을 대신할 수 없다.
가능하면 에뮬레이터나 연결된 Android 기기에 설치해 실행까지 확인해라. 기기 실행이 불가능해도 Debug APK 빌드와 단위 테스트를 시도하고, 실제 검증 여부를 정확히 나눠 보고해라.
첫 APK를 전달한 뒤 문서의 단계 순서대로 기능을 확장하고, 각 단계가 빌드되고 검증되는지 확인해라.
모호한 부분은 임의로 외부 서비스나 유료 API를 추가하지 말고 TODO와 선택지를 보고해라.
UI는 제공한 레퍼런스의 정보 구조와 밀도를 참고하되 브랜드 자산과 화면을 픽셀 단위로 복제하지 마라.
애니메이션은 문서의 Motion System을 공통 토큰으로 구현하고, 접근성의 애니메이션 감소 설정도 지원해라.
API 키와 비밀 값은 APK에 넣지 말고 서버 환경변수에만 저장해라.
각 작업 후 변경 파일, 구현 내용, 테스트 결과, APK 위치, 남은 문제를 초보자도 이해할 수 있는 한국어로 보고해라.
```

### 첫 Debug APK 빌드·실행 확인

프로젝트에 Gradle Wrapper가 이미 있으면 반드시 그것을 사용한다. Windows에서는 `.\gradlew.bat assembleDebug`, macOS/Linux에서는 `./gradlew assembleDebug`를 실행한다. 기본 출력 위치는 `app/build/outputs/apk/debug/app-debug.apk`지만 실제 모듈 이름과 경로가 다르면 생성된 경로를 그대로 보고한다.

기기나 에뮬레이터가 연결되어 있으면 해당 APK를 설치하고 실행한다. 예: `adb install -r <APK 경로>` 후 Android Studio의 Run 또는 실제 launcher activity로 실행. 설치 대상을 사용할 수 없으면 APK 빌드와 테스트까지만 수행했다고 명시하고, Android Studio에서 여는 방법을 안내한다. 실행하지 않은 것을 실행했다고 주장하지 않는다.

---

## 1. 제품 목표

### 1.1 한 문장 정의

자연어·영수증·결제 알림으로 지출을 빠르게 기록하고, 사용자가 자신의 소비 데이터에 관해 대화할 수 있는 모바일 AI 가계부다.

### 1.2 핵심 가치

1. **3초 기록**: `어제 점심 8천원`처럼 말하거나 입력하면 거래 후보가 완성된다.
2. **정확한 숫자**: 합계와 통계는 AI가 아니라 코드와 DB가 계산한다.
3. **이해 가능한 분석**: AI는 계산된 통계를 사람에게 유용한 문장으로 설명한다.
4. **개인/공동 분리**: 개인 소비는 숨기고 선택한 거래만 파트너와 공유할 수 있다.
5. **차분하지만 살아 있는 UI**: 과한 효과 없이 이동 관계와 상태 변화를 자연스럽게 보여준다.

### 1.3 범위 밖

- Weeple의 이름, 로고, 아이콘 파일, 카피 문구, 일러스트를 가져오지 않는다.
- APK 디컴파일, 네트워크 가로채기 등 역공학으로 구현을 추출하지 않는다.
- 첫 버전에서 은행 계좌에 직접 접속하거나 송금 기능을 제공하지 않는다.
- AI가 DB에 거래를 확인 없이 직접 저장하거나 수정·삭제하지 않는다.
- 투자 조언, 신용평가, 대출 추천을 핵심 기능으로 만들지 않는다.

---

## 2. 제공된 레퍼런스 분석

분석 자료:

- 1080 × 2520 세로 스크린샷 1장
- 약 15초, 1080 × 2520, 120fps 화면 녹화 1개

### 2.1 확인된 화면 구조

| 영역 | 관찰된 구조 | 우리 앱 적용 방향 |
|---|---|---|
| 상단 | 프로필 원형 아바타, 인사말, 날짜, 도움말·알림·설정 | 프로필/기간 문맥과 빠른 설정 유지 |
| 빠른 입력 | 큰 둥근 입력 카드, 음성·카메라·이미지 버튼 | 자연어 입력의 주 진입점으로 유지 |
| 상단 탭 | 요약·인사이트·리포트·AI 분석 | 스크롤 가능한 분석 탭 또는 화면별 탭 |
| 요약 | 월 지출, 증감, 예산 진행 바, 예상 지출, 오늘/주간 카드 | 홈의 핵심 숫자는 한눈에 보이게 구성 |
| 소비내역 | 월간 캘린더, CSV, 수입/지출 필터, 검색, 날짜별 목록 | 캘린더와 리스트가 같은 선택 날짜를 공유 |
| 커플 | 초대 코드, QR, 공유 대시보드·목표·리포트·AI | 연결 전/후 상태를 분리 |
| 관리 | 결제일, 카드, 계좌, 고정비, 카테고리, 예산 | 설정 목록을 의미 단위 카드로 그룹화 |
| 하단 탐색 | 4개 탭 + 중앙 대형 추가 버튼 | 4대 목적지와 글로벌 추가 동작 유지 |
| 추가 메뉴 | FAB가 X로 바뀌고 입력 패널 및 액션이 나타남 | 배경 scrim + 확장 패널 + 키보드 연동 |

### 2.2 영상에서 확인된 모션 특성

- 하단 탭 이동 시 새 화면이 대부분 즉시 교체된다.
- 활성 탭의 배경과 색은 바뀌지만 아이콘의 이동 또는 자연스러운 선택 표시 이동은 거의 없다.
- 요약 숫자, 예산 바, 카테고리 바가 정적인 상태로 등장한다.
- 커플과 관리 화면의 큰 카드도 별도 등장 모션이 거의 없다.
- 중앙 `+`를 누르면 입력 패널이 열리고 FAB가 닫기 상태로 바뀌지만, 전체적인 전환 연속성은 약하다.
- 키보드가 올라올 때 콘텐츠와 패널의 관계가 다소 갑작스럽다.

따라서 **레퍼런스의 정보 구조는 참고하되 모션의 딱딱함은 복제하지 않는다.** 기능 개발 초반에는 최소 전환만 넣고, 핵심 플로우가 안정된 후 Motion Polish 단계에서 아래 공통 시스템을 일괄 적용한다.

---

## 3. 기술 스택

### Android

- Kotlin
- Jetpack Compose + Material 3
- MVVM + 단방향 데이터 흐름(UDF)
- Navigation Compose
- Room
- DataStore
- Hilt
- WorkManager
- Retrofit/OkHttp 또는 Ktor Client
- Kotlin Coroutines + Flow
- ML Kit Text Recognition
- NotificationListenerService

### 서버

- Supabase Auth, PostgreSQL, Realtime, Storage, Row Level Security
- AI 키를 보호할 서버 함수 또는 별도 FastAPI 서버
- API 응답에 명시적인 버전과 오류 코드를 포함

### AI

- 기본: Gemini Flash-Lite급 경량 모델 — 자연어 거래 파싱, 짧은 분류
- 분석: Gemini Flash급 모델 — 소비 분석, 가계부 Q&A
- OCR: ML Kit 로컬 인식 후 필요할 때만 AI가 구조화
- 공급자 인터페이스를 추상화해 모델 교체 가능하게 구현
- 실제 모델명과 무료 한도는 구현 시점 공식 문서를 다시 확인

### 보안 원칙

- AI API 키를 `BuildConfig`, 리소스, 소스 코드, APK에 포함하지 않는다.
- 앱은 HTTPS로 우리 서버만 호출한다.
- 서버는 사용자 인증과 사용량 제한을 확인한 뒤 AI API를 호출한다.
- AI에 카드번호, 계좌번호, 사용자 이름을 보내지 않는다.
- 영수증 원본 전송은 명시적 동의를 받고 최소 기간만 보관한다.

---

## 4. 앱 정보 구조

### 하단 탐색

1. **홈**: 이번 달 요약, 예산, 오늘/주간, 주요 카테고리, AI 한 줄 인사이트
2. **내역**: 캘린더, 거래 목록, 검색, 필터, CSV
3. **중앙 추가**: 직접 입력, AI 입력, 음성, 영수증, 반복 거래
4. **함께**: 파트너 연결, 공동 지출, 공동 목표, 공동 분석
5. **관리**: 결제수단, 카테고리, 예산, 고정 거래, 알림, 앱 설정

### 주요 화면 목록

1. 스플래시/세션 복원
2. 온보딩
3. 로그인/회원가입/비밀번호 재설정
4. 홈 요약
5. 홈 인사이트
6. 홈 리포트
7. AI 분석
8. 소비내역 캘린더
9. 거래 검색/필터
10. 거래 상세/수정
11. 거래 추가 방식 선택
12. 직접 거래 입력
13. 자연어 AI 입력 및 확인
14. 영수증 촬영/가져오기/검토
15. 알림 감지 거래 후보함
16. 예산 목록/편집
17. 카테고리 관리
18. 카드·계좌 관리
19. 반복 거래/고정비
20. 파트너 연결 전
21. 초대 코드/QR
22. 공동 대시보드
23. 공동 거래/목표/리포트
24. 알림 센터
25. 설정/테마/개인정보/데이터 내보내기

---

## 5. 디자인 시스템

### 5.1 브랜드 방향

레퍼런스의 어두운 청록 계열과 카드 중심 정보 밀도는 참고하되, 앱 이름·로고·아이콘·문구는 독자적으로 제작한다. 임시 이름도 코드 전체에 하드코딩하지 말고 리소스로 분리한다.

### 5.2 의미 기반 색 토큰

```kotlin
data class FinanceColors(
    val accent: Color,
    val accentSoft: Color,
    val income: Color,
    val expense: Color,
    val warning: Color,
    val success: Color,
    val surfaceBase: Color,
    val surfaceRaised: Color,
    val surfaceOverlay: Color,
    val textPrimary: Color,
    val textSecondary: Color,
    val divider: Color
)
```

테마 후보: Mint, Blue, Purple, Coral, Mono. 사용자가 테마를 바꾸면 FAB, 선택 탭, 차트, 진행률, 링크, 포커스 링이 함께 바뀐다. 금액 의미 색은 테마와 분리해 수입/지출 혼동을 막는다.

### 5.3 크기 규칙

- 기본 간격 단위: 4dp
- 화면 좌우 여백: 20dp(소형 화면은 16dp)
- 카드 내부 여백: 16~20dp
- 카드 모서리: 기본 20dp, 작은 칩 12dp, 큰 입력 카드 24dp
- 최소 터치 영역: 48 × 48dp
- 하단 바는 시스템 제스처 inset을 포함
- 고정 높이 남용 금지, 폰트 크기 확대와 Fold 펼침 화면 대응

### 5.4 타이포그래피

- 월 핵심 금액: 38~44sp, 굵게, tabular figures
- 화면 제목: 24~28sp
- 카드 핵심값: 20~24sp
- 본문: 15~16sp
- 보조 정보: 12~14sp
- 원화 기호와 숫자 baseline을 맞추고 금액 변경 중 레이아웃 폭이 튀지 않게 한다.

### 5.5 아이콘과 접근성

- 한 아이콘 세트(Material Symbols Rounded 또는 직접 제작한 단일 세트)만 사용
- 컬러 이모지를 기능 아이콘으로 쓰지 않음
- TalkBack 설명, 의미 있는 선택 상태, 색 이외의 구분 제공
- 명암비와 동적 글꼴 크기 검증

---

## 6. Motion System — 후반 품질 개선의 핵심

### 6.1 원칙

1. 애니메이션은 장식이 아니라 **어디서 어디로 이동했는지** 설명한다.
2. 입력 반응은 빠르게, 화면 이동은 부드럽게, 축하 효과는 짧게 만든다.
3. 모든 화면이 제각각 움직이지 않도록 공통 토큰을 사용한다.
4. 동시에 움직이는 큰 요소는 3개를 넘기지 않는다.
5. 스크롤 중 계속 반복되는 애니메이션은 피한다.
6. 데이터가 갱신될 때 기존 UI 전체를 깜빡이지 않는다.

### 6.2 공통 시간 토큰

| 토큰 | 시간 | 용도 |
|---|---:|---|
| Instant | 90ms | 눌림, 작은 색 변화 |
| Fast | 160ms | 칩, 아이콘, 토글 |
| Standard | 240ms | 카드, 탭 콘텐츠, 숫자 |
| Emphasized | 360ms | 화면 전환, 바텀시트, FAB 변형 |
| Delight | 520ms 이하 | 첫 예산 달성 등 드문 긍정 피드백 |

표의 값은 영상에서 추출한 Weeple 내부 수치가 아니라 우리 앱을 위해 정한 구현 기준이다.

### 6.3 easing

- 화면 진입: `FastOutSlowInEasing`
- 화면 퇴장: `FastOutLinearInEasing`
- 사용자가 직접 드래그하는 시트: spring, `dampingRatio = 0.82f`, 낮은 bounce
- 숫자와 진행 바: 부드러운 감속, overshoot 금지
- 큰 카드에 과도한 elastic/bounce 금지

### 6.4 컴포넌트별 규격

#### 하단 탭

- 선택 indicator가 기존 탭에서 새 탭으로 240ms 동안 이동한다.
- 선택 아이콘은 0.92 → 1.0 scale, 색상은 160ms 전환한다.
- 탭 콘텐츠는 진행 방향에 따라 12dp 옆 이동 + fade, 총 240ms.
- 매번 화면 전체를 재생성하는 느낌이 나지 않도록 상단/하단 chrome은 유지한다.
- 연속으로 빠르게 누르면 이전 애니메이션을 안전하게 취소하고 최신 탭으로 수렴한다.

#### 중앙 추가 FAB

- 원형 FAB는 제자리에서 45도 회전해 `+`가 `×` 형태로 변한다.
- 색 변화 160ms, 회전·크기 변화 240ms.
- 배경 scrim은 0 → 0.46 alpha로 200ms 전환한다.
- 액션 패널은 FAB의 위치를 기점으로 아래에서 16dp 상승하며 fade/scale(0.96 → 1.0), 280ms.
- 직접 입력·AI·음성·영수증 액션은 35ms stagger로 등장한다.
- 뒤로가기, scrim 탭, 닫기 버튼은 모두 같은 종료 상태를 사용한다.
- 키보드가 열릴 때 패널이 순간이동하지 않도록 IME inset을 따라 animate한다.

#### 홈 데이터

- 월 변경 시 금액은 0부터 다시 세지 말고 이전 값에서 새 값으로 300ms 보간한다.
- 예산 진행 바와 카테고리 바는 왼쪽에서 360ms 채워진다.
- `₩`, 쉼표, 숫자 폭이 튀지 않도록 tabular figures 적용.
- 카드 2개는 30ms stagger로 fade + 8dp 상승.
- 이미 본 화면으로 돌아올 때 모든 등장 모션을 반복하지 않는다.

#### 캘린더/소비내역

- 월 전환은 방향성 있는 horizontal slide 280ms.
- 날짜 선택 indicator는 새 날짜로 이동하며 색/크기 200ms 전환.
- 날짜 변경 시 목록은 crossfade 180ms; 스크롤 위치 정책을 명확히 한다.
- 필터 칩 선택은 배경·텍스트 색과 체크 아이콘을 함께 변경한다.
- 거래 추가 시 해당 날짜 목록에 240ms expand + fade.
- 삭제는 즉시 완전 제거하지 않고 220ms collapse 후 Snackbar에서 실행 취소 제공.

#### 거래 상세/편집

- 목록 카드와 상세 상단 카드 사이에 shared element 느낌의 연속성을 준다.
- 저장 성공 시 버튼이 160ms 동안 체크 상태로 바뀐 뒤 화면을 닫는다.
- 필드 오류는 흔들기보다 테두리/설명 문구 fade를 사용한다.

#### AI 입력/분석

- 전송 즉시 사용자 문장은 고정하고 구조화 후보 카드 skeleton을 표시한다.
- skeleton shimmer는 저대비로 제한하고 1.2초 주기, 3회 이후 정적 상태로 전환한다.
- 추출된 금액·날짜·카테고리는 40ms stagger로 나타난다.
- 확신도가 낮은 필드만 강조하고 저장 전 확인을 요구한다.
- 분석 텍스트를 한 글자씩 느리게 출력하지 않는다. 의미 단락 단위 fade-in을 사용한다.
- 스트리밍 중 취소 버튼과 재시도 상태를 제공한다.

#### 커플 연결

- 연결 전과 연결 후는 동일 화면에서 `AnimatedContent`로 상태 전환한다.
- QR 생성 성공은 카드가 0.96 → 1.0 scale로 안정적으로 나타난다.
- 상대방 연결 성공 시 520ms 이내의 절제된 체크/링 효과를 한 번만 실행한다.

#### 설정/관리

- 섹션은 화면 진입마다 줄줄이 날아오지 않는다.
- 토글과 선택 상태만 160ms로 반응한다.
- 테마 변경은 전체 화면 색상을 300ms crossfade하고 레이아웃은 유지한다.

### 6.5 시스템 뒤로가기와 제스처

- Android Predictive Back 지원.
- 바텀시트, 팝업, 키보드, 화면 순서로 닫힌다.
- 제스처 진행률과 화면 scale/translation을 연결한다.
- 중간에 취소하면 원래 상태로 자연스럽게 복귀한다.

### 6.6 애니메이션 감소

- 시스템 animator duration scale이 0이거나 앱의 `모션 줄이기`가 켜지면 이동·scale·stagger를 제거한다.
- 정보 상태 변화는 80~120ms crossfade 또는 즉시 전환으로 유지한다.
- 기능과 의미가 애니메이션에만 의존하지 않게 한다.

### 6.7 Compose 구현 규칙

- duration/easing을 화면 파일에 숫자로 흩뿌리지 말고 `FinanceMotion`에 집중한다.
- `AnimatedContent`, `AnimatedVisibility`, `animate*AsState`, `updateTransition`을 목적에 맞게 사용한다.
- 복잡한 순차 모션만 `Animatable`을 사용한다.
- LazyColumn item에는 안정적인 key를 지정한다.
- 애니메이션 중 DB 재조회로 상태가 초기화되지 않게 ViewModel state를 유지한다.
- 120Hz 기기에서도 jank가 없도록 release build에서 Macrobenchmark와 JankStats로 검증한다.

```kotlin
object FinanceMotion {
    const val Instant = 90
    const val Fast = 160
    const val Standard = 240
    const val Emphasized = 360

    val enter = FastOutSlowInEasing
    val exit = FastOutLinearInEasing
    val sheetSpring = spring<Float>(
        dampingRatio = 0.82f,
        stiffness = Spring.StiffnessMediumLow
    )
}
```

---

## 7. 핵심 기능 명세

### 7.1 거래

필수 필드:

- id, ownerId, ledgerId
- type: EXPENSE / INCOME / TRANSFER
- amount: 정수 최소 화폐 단위
- currency
- occurredAt, timezone
- categoryId
- merchant, memo
- paymentMethodId
- source: MANUAL / NATURAL_LANGUAGE / RECEIPT / NOTIFICATION / RECURRING
- sharingScope: PRIVATE / SHARED
- createdAt, updatedAt, deletedAt

규칙:

- 금액은 부동소수점으로 저장하지 않는다.
- 삭제는 동기화를 고려해 soft delete한다.
- 이체는 지출 통계에서 제외하며 출발/도착 계정을 연결한다.
- AI/OCR/알림 결과는 곧바로 확정 거래가 아니라 후보로 만든다.

### 7.2 예산과 통계

- 전체 월 예산과 카테고리 예산
- 이월 여부
- 남은 예산, 하루 사용 가능 금액
- 지난달 동일 기간 비교
- 월말 예상 지출
- 카테고리 TOP
- 고정비/변동비 분리

모든 수치는 SQL 또는 Kotlin의 결정적 계산 함수에서 구한다. 테스트 fixture로 동일 입력에 항상 동일 결과가 나와야 한다.

### 7.3 자연어 거래 입력

흐름:

1. 사용자가 `어제 친구랑 치킨 24000원` 입력
2. 서버가 날짜·사용자 locale·허용 카테고리와 함께 모델 호출
3. JSON schema 검증
4. 앱에 후보 표시
5. 사용자가 수정/확인
6. 확정 저장

응답 예시:

```json
{
  "schemaVersion": 1,
  "type": "EXPENSE",
  "amount": 24000,
  "currency": "KRW",
  "occurredDate": "2026-09-14",
  "categoryKey": "FOOD",
  "merchant": "치킨",
  "memo": "친구와 함께",
  "confidence": {
    "amount": 0.99,
    "date": 0.96,
    "category": 0.84
  },
  "needsConfirmation": []
}
```

금액이 없거나 날짜가 모호하면 추측하여 저장하지 않고 필요한 항목만 물어본다.

### 7.4 영수증 OCR

1. 카메라/갤러리 권한 설명
2. 이미지 회전·크롭·대비 보정
3. ML Kit 로컬 OCR
4. 상호·날짜·총액 후보 파싱
5. 여러 총액 후보가 있으면 사용자가 선택
6. 필요할 때만 최소 텍스트를 AI에 보내 구조화
7. 검토 후 저장

### 7.5 Android 결제 알림

- NotificationListenerService는 사용자가 설정 화면에서 직접 허용한다.
- 지원 앱/은행별 로컬 parser를 분리한다.
- 원문, 패키지명, 시간, 금액, 가맹점 fingerprint로 중복 후보 방지.
- 확신도가 낮으면 후보함에만 보관.
- 알림 내용을 서버나 AI로 보낼 때 별도 동의와 마스킹 적용.

### 7.6 AI 분석과 Q&A

서버가 먼저 아래 데이터를 계산한다.

- 기간별 합계
- 카테고리별 합계/비율/증감
- 상위 가맹점
- 반복 패턴
- 예산 소진율
- 최근 7일 속도와 월말 예상값

AI는 이 숫자를 바꾸지 않고 다음만 수행한다.

- 눈에 띄는 변화 설명
- 긍정 신호와 주의 신호 구분
- 실행 가능한 절약 제안
- 질문 의도 파악 및 필요한 조회 도구 선택

AI 답변에는 기준 기간과 사용한 실제 숫자를 표시한다. 데이터가 없으면 모른다고 답하고 거래를 만들어내지 않는다.

### 7.7 개인/공동 가계부

- 사용자는 기본 개인 ledger를 가진다.
- 초대 코드/QR은 짧은 만료시간과 1회 사용을 적용한다.
- 개인 거래는 기본 PRIVATE.
- 사용자가 명시적으로 SHARED로 바꾼 거래만 공동 화면에 노출.
- 공동 예산과 공동 목표는 멤버별 수정 권한을 적용.
- 연결 해제 시 개인 데이터와 공동 데이터 처리 방식을 확인받는다.

---

## 8. 데이터 모델 초안

```text
profiles
ledgers
ledger_members
transactions
transaction_candidates
categories
payment_methods
budgets
recurring_rules
notification_rules
receipts
shared_goals
ai_analysis_runs
ai_usage_events
user_settings
```

모든 공유 테이블에 `ledger_id`와 RLS 정책을 적용한다. 사용자가 속하지 않은 ledger는 읽기와 쓰기 모두 불가능해야 한다. 로컬 Room에는 서버 ID, 동기화 상태, 수정 버전, 삭제 시각을 저장한다.

---

## 9. 오프라인과 동기화

- 거래 조회·추가·수정은 오프라인 우선.
- Room을 UI의 single source of truth로 사용.
- 네트워크 복구 시 WorkManager가 outbox를 순서대로 전송.
- 충돌은 서버 revision과 updatedAt을 함께 사용하며 중요한 금액 충돌은 사용자에게 선택 요청.
- AI와 OCR 서버 호출이 실패해도 직접 입력은 계속 사용할 수 있어야 한다.
- 동기화 중이라는 이유로 전체 화면 로딩을 띄우지 않는다.

---

## 10. 오류·빈 상태·로딩 상태

각 화면은 다음 상태를 명시적으로 가진다.

- Loading: skeleton 또는 기존 데이터 + 작은 진행 표시
- Content
- Empty: 다음 행동이 분명한 안내
- Partial: 일부 카드만 실패
- Offline: 로컬 데이터 유지 + 재시도
- Error: 사용자 문장 + 진단 가능한 내부 코드

AI 한도 초과, 서버 장애, 인증 만료, OCR 실패를 일반 `오류가 발생했습니다` 하나로 처리하지 않는다.

---

## 11. 개인정보 및 사용자 통제

- 알림 접근, 카메라, 사진 권한은 기능을 누를 때 요청한다.
- 권한을 거부해도 직접 입력은 사용 가능하다.
- AI로 전송되는 데이터 종류와 목적을 설정에서 확인할 수 있다.
- 거래 데이터 내보내기(CSV/JSON)와 계정 삭제 제공.
- 로그에 원문 거래, 영수증 텍스트, 토큰, API 키를 기록하지 않는다.
- 분석용 이벤트에는 금액/가맹점 대신 이벤트 유형과 성공 여부만 기록한다.

---

## 12. 구현 단계

### Phase 0 — 기존 코드 감사 후 즉시 착수

- 프로젝트 구조, Gradle, 현재 화면, 데이터 모델, 미완성 코드를 검사
- 동작하는 부분과 변경 위험을 보고
- 기준 스크린샷 테스트와 빌드 명령 확보
- Android 프로젝트가 없으면 실제 프로젝트를 생성
- 감사 결과를 짧게 기록한 뒤 같은 작업 흐름에서 Phase 1 구현을 시작한다. 분석 보고서만 내고 멈추지 않는다.

### Phase 1 — 첫 실행 가능한 앱 (vertical slice)

- Android Manifest와 launcher activity를 포함한 실제 앱 진입점
- Compose 테마, 타이포그래피, 의미 색, 간격, 공통 카드
- Navigation과 실제 화면이 연결된 하단 바, 중앙 FAB
- Room Entity/DAO/Database/Repository와 기본 거래 입력·저장·조회
- 홈 요약과 내역 화면이 같은 실제 로컬 거래 데이터를 표시
- 라이트/다크 및 기본 접근성
- Debug APK 생성, 단위 테스트, 가능하면 기기/에뮬레이터 설치와 실행

완료 조건: `assembleDebug` 성공, APK 생성, 홈/내역 탭 이동, 거래를 추가한 뒤 앱을 재실행해도 Room 데이터가 남아 있음, 시스템 inset 정상. 기기를 사용할 수 있으면 설치 후 첫 화면과 거래 입력까지 직접 확인한다. 네트워크/API 키 없이도 이 단계는 작동해야 한다.

### Phase 2 — 로컬 가계부 MVP

- 거래 수정/삭제와 Undo
- 홈 요약 확장, 내역 캘린더/목록
- 카테고리, 예산, 결제수단
- 통계 계산 테스트

완료 조건: 인터넷 없이 한 달 사용 가능, 합계 테스트 전부 통과.

### Phase 3 — 인증·서버·동기화

- Supabase 인증과 RLS
- outbox 동기화
- 다른 기기 세션 복원

완료 조건: 두 기기에서 같은 계정 데이터 동기화, 다른 사용자 데이터 접근 차단 테스트.

### Phase 4 — 빠른 입력

- 자연어 AI 파싱
- ML Kit OCR
- NotificationListenerService와 후보함
- 모든 자동 결과 확인 화면

완료 조건: 잘못된 후보가 사용자 확인 없이 원장에 저장되지 않음.

### Phase 5 — AI 분석/비서

- 결정적 통계 payload
- 주간/월간 분석
- 자연어 Q&A와 조회 도구
- 사용량 제한, 캐시, 오류 처리

완료 조건: AI가 제시한 금액이 서버 계산값과 일치하며 근거 기간 표시.

### Phase 6 — 공동 가계부

- 초대 코드/QR
- 개인/공동 scope
- 공동 예산·목표·리포트
- 연결 해제 정책

### Phase 7 — Motion Polish

이 단계에서 본 문서의 Motion System을 전체 적용한다. 초반 임시 애니메이션을 화면마다 덧붙이지 말고 공통 토큰과 컴포넌트로 교체한다.

- 탭 indicator와 방향 전환
- FAB morph, scrim, 입력 패널, IME 동기화
- 숫자·진행률·차트 전환
- 목록 삽입/삭제/Undo
- AI skeleton → 결과
- predictive back
- 모션 줄이기
- 60/120Hz 실기기 jank 검사

완료 조건:

- 주요 상호작용에서 느껴지는 끊김 없음
- 프레임 손실과 recomposition hotspot 기록
- 빠른 연타·뒤로가기·화면 회전에도 잘못된 중간 상태 없음
- TalkBack과 모션 감소 모드에서 기능 동일

### Phase 8 — 출시 준비

- 개인정보처리방침과 이용약관
- 데이터 삭제/내보내기
- Crash reporting, 비식별 analytics
- Play Console 내부 테스트
- 다양한 화면, 폰트 크기, 저사양 기기 테스트

---

## 13. 테스트 체크리스트

### 기능

- [ ] 원화 금액 쉼표와 음수/0원 검증
- [ ] 월 경계, 윤년, 시간대 변경
- [ ] 수입/지출/이체 통계 분리
- [ ] 알림 중복 방지
- [ ] OCR의 여러 총액 후보
- [ ] AI JSON schema 실패와 재시도
- [ ] 개인 거래가 공동 사용자에게 노출되지 않음
- [ ] 오프라인 수정 후 충돌

### UI/모션

- [ ] 하단 탭 빠른 연타
- [ ] FAB 열림 중 뒤로가기
- [ ] FAB/키보드 열림 상태에서 회전
- [ ] 월 변경 중 데이터 응답 도착
- [ ] 목록 삭제 애니메이션 중 Undo
- [ ] 60Hz/120Hz 실제 기기
- [ ] 배터리 절약 모드
- [ ] animator scale 0×, 0.5×, 1×, 10×
- [ ] 큰 글씨 200%
- [ ] TalkBack 탐색 순서
- [ ] Galaxy Z Fold 접힘/펼침과 창 크기 변경

### 성능 기준

- 앱 시작 시 불필요한 AI/API 호출 0회
- 탭 전환 중 메인 스레드 DB/네트워크 작업 0회
- 목록에 stable key 사용
- 이미지 decode 크기 제한
- release 빌드의 baseline profile 및 macrobenchmark 작성

---

## 14. Codex 작업 규칙

1. 기존 사용자 코드를 먼저 읽고 관련 없는 변경은 하지 않는다.
2. 매 단계 시작 전 수정할 파일과 완료 조건을 짧게 보고한다.
3. 큰 단일 파일로 모든 기능을 몰아넣지 않는다.
4. 화면에 임시 숫자를 하드코딩한 채 완료라고 하지 않는다.
5. 실제 서버가 아직 없으면 fake repository를 인터페이스 뒤에 두고 명확히 표시한다.
6. 컴파일만 확인하지 말고 unit test, UI test, lint를 실행한다.
7. 권한이나 비밀 키가 없어서 검증 불가능한 것은 숨기지 말고 정확히 보고한다.
8. 사용자에게 필요한 설정은 복사·붙여넣기 가능한 순서로 설명한다.
9. 각 Phase가 끝날 때 `IMPLEMENTATION_STATUS.md`를 갱신한다.
10. 앱 브랜드가 확정되기 전 Weeple 이름을 패키지명이나 최종 UI에 사용하지 않는다.

---

## 15. 최종 완성 기준

완성은 화면이 비슷해 보이는 상태가 아니다. 다음 조건을 모두 만족해야 한다.

- 사용자는 세 번 이하의 주요 조작으로 거래를 기록할 수 있다.
- 자동 인식 결과는 확인 가능하고 잘못된 값을 쉽게 고칠 수 있다.
- 모든 합계와 AI가 말하는 금액이 실제 DB 값과 일치한다.
- 개인/공동 데이터 경계가 서버 정책으로 보호된다.
- 오프라인에서도 핵심 가계부가 작동한다.
- 화면 전환과 데이터 변화가 부드럽지만 방해되지 않는다.
- 모션을 줄여도 기능과 정보가 손실되지 않는다.
- 실제 Galaxy 기기와 Fold 화면에서 안정적으로 동작한다.
- API 키가 앱에 포함되지 않는다.
- 앱은 레퍼런스와 비슷한 친숙함을 주되 독자적인 브랜드와 자산을 가진다.

이 문서의 우선순위는 **정확성 → 개인정보/보안 → 핵심 사용성 → 성능 → 모션과 시각적 완성도 → 부가 기능** 순서다. 단, Motion System을 처음부터 공통 토큰으로 준비해두고 실제 전체 적용은 Phase 7에서 수행한다.
